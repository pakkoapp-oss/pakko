# AGENT.md — Redirect

> **Superseded 2026-07-05.** This file used to be a second, competing entry point (its own
> "Read Order", its own hard-constraints subset, and dangling references to `RESEARCH.md` /
> `windows-archiver-reality-2026.md` — neither of which exists in this repo). It duplicated and
> drifted from `CLAUDE.md` (last known state here: v1.0, 45 tests, no mention of `Archiver.Shell`/
> `Archiver.ShellExtension`/`DIAGRAMS.md`). Kept as a stub rather than deleted, per this project's
> "never silently deprecate a document" rule.

**Read `CLAUDE.md` instead** — it now contains the full documentation map (which file to read/
update for any given change), the current hard constraints, current project status, and build
commands. `CLAUDE.md`'s "Documentation Map" section is the single index for this repo; do not
maintain a second one here.

If a non-Claude coding agent (e.g. OpenAI Codex) ever needs its own distinct entry point again,
recreate that content here deliberately — don't let it silently re-diverge from `CLAUDE.md`.
